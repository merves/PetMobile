<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trk.petmobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignActivity" />
        <activity
            android:name=".AnamenuActivity"
            android:label="@string/title_activity_anamenu"
            android:parentActivityName=".SignActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.trk.petmobile.SignActivity" />
        </activity>
        <activity android:name=".PetInfoActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".VaccineActvity" />
        <activity android:name=".ThreatmentActivity" />
        <activity android:name=".AppointmentActivity"></activity>
    </application>

</manifest>